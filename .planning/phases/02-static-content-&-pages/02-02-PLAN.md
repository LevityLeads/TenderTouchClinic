---
phase: 02-static-content-and-pages
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/app/page.tsx
  - src/components/sections/hero.tsx
  - src/components/sections/services-overview.tsx
  - src/components/sections/about-intro.tsx
  - src/components/sections/testimonials.tsx
  - src/components/sections/cta-section.tsx
  - src/components/sections/trust-indicators.tsx
  - src/app/services/page.tsx
  - src/app/services/antenatal-classes/page.tsx
  - src/app/services/baby-massage/page.tsx
  - src/app/services/postnatal-support/page.tsx
  - src/components/ui/card.tsx
  - src/components/ui/section.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor on homepage can immediately see services overview and understand what Tender Touch offers"
    - "Visitor can navigate to any service page and find pricing, course details, and benefits"
    - "Visitor can see testimonials from previous clients on homepage"
    - "Visitor can see Megan's credentials and trust indicators on homepage"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Homepage with all sections"
      contains: ["Hero", "ServicesOverview", "AboutIntro", "Testimonials", "CTASection", "TrustIndicators"]
    - path: "src/app/services/page.tsx"
      provides: "Services overview page"
      contains: "services.map"
    - path: "src/app/services/antenatal-classes/page.tsx"
      provides: "Antenatal classes detail page"
      contains: ["pricing", "benefits", "Book"]
    - path: "src/app/services/baby-massage/page.tsx"
      provides: "Baby massage detail page"
      contains: ["pricing", "benefits", "Book"]
    - path: "src/app/services/postnatal-support/page.tsx"
      provides: "Postnatal support detail page"
      contains: ["pricing", "benefits", "Contact"]
    - path: "src/components/sections/hero.tsx"
      provides: "Hero section with CTA"
      contains: ["priority", "Book"]
    - path: "src/components/sections/testimonials.tsx"
      provides: "Testimonials grid"
      contains: "testimonials"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/sections/*.tsx"
      via: "import"
      pattern: "import.*from.*@/components/sections"
    - from: "src/components/sections/services-overview.tsx"
      to: "src/data/services.ts"
      via: "import"
      pattern: "import.*services.*from.*@/data/services"
    - from: "src/components/sections/testimonials.tsx"
      to: "src/data/testimonials.ts"
      via: "import"
      pattern: "import.*testimonials.*from.*@/data/testimonials"
---

<objective>
Create the homepage with all sections and the services pages (overview + 3 detail pages).

Purpose: Deliver the core visitor experience where potential clients can understand Tender Touch's offerings, see social proof through testimonials, and navigate to detailed service information with pricing and booking CTAs.

Output: Fully functional homepage with 6 sections, services overview page, and 3 service detail pages (antenatal, baby massage, postnatal support).
</objective>

<execution_context>
@/home/codespace/.claude/get-shit-done/workflows/execute-plan.md
@/home/codespace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-static-content-&-pages/02-RESEARCH.md

# Phase 1 patterns
@src/components/ui/button.tsx
@src/components/ui/container.tsx
@src/lib/constants.ts

# Phase 2 Plan 01 outputs (must complete first)
@src/data/services.ts
@src/data/testimonials.ts
@src/data/about.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UI primitives and homepage section components</name>
  <files>
    src/components/ui/card.tsx
    src/components/ui/section.tsx
    src/components/sections/hero.tsx
    src/components/sections/services-overview.tsx
    src/components/sections/about-intro.tsx
    src/components/sections/testimonials.tsx
    src/components/sections/cta-section.tsx
    src/components/sections/trust-indicators.tsx
  </files>
  <action>
1. Create src/components/ui/card.tsx:
   - Server Component (no 'use client')
   - Export Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter
   - Use neutral border, rounded-lg, hover shadow transition
   - Follow existing button.tsx pattern with cn() for class merging

2. Create src/components/ui/section.tsx:
   - Server Component
   - Props: id?, className?, children, variant ('default' | 'muted' | 'primary')
   - Default variant: white background
   - Muted variant: neutral-50 background
   - Primary variant: primary-50 background
   - Standard padding: py-16 lg:py-24

3. Create src/components/sections/hero.tsx (HOME-01):
   - Server Component
   - Full-width hero with background gradient (no hero image initially - use gradient overlay)
   - Headline: "Nurturing Care for Mothers & Babies"
   - Subheadline about services and Cape Town location
   - Two CTAs: "Book a Class" (primary) linking to /schedule, "View Services" (outline) linking to /services
   - Use min-h-[60vh] for visual impact
   - Use existing Button component with href prop

4. Create src/components/sections/services-overview.tsx (HOME-02):
   - Server Component
   - Import services from @/data/services
   - Display grid (1 col mobile, 2 cols sm, 3 cols lg) of service cards
   - Each card shows: name, shortDescription, "Learn more" link
   - Use Card component for each service
   - Link to service.ctaHref for each

5. Create src/components/sections/about-intro.tsx (HOME-03):
   - Server Component
   - Import meganBio from @/data/about
   - Brief intro to Megan (2-3 sentences from bio)
   - Credentials list (first 2-3 items)
   - "Read more" link to /about
   - Two-column layout on lg (text + placeholder for image)

6. Create src/components/sections/testimonials.tsx (HOME-04, TEST-01 through TEST-05):
   - Server Component
   - Import testimonials from @/data/testimonials
   - Display 3 testimonials in responsive grid (1 col mobile, 3 cols lg)
   - Each testimonial shows: quote (blockquote), name (cite), service used
   - Use quotation styling with blockquote semantics
   - Placeholder avatar using first letter of name if no imageUrl

7. Create src/components/sections/cta-section.tsx (HOME-05):
   - Server Component
   - Centered text with compelling headline ("Ready to Begin Your Journey?")
   - Two CTAs: "Book Your Class" (primary) to /schedule, "Contact Us" (secondary) to /contact
   - Use primary-50 background for visual distinction

8. Create src/components/sections/trust-indicators.tsx (HOME-06):
   - Server Component
   - Import meganBio from @/data/about
   - Display credentials in horizontal row on lg, vertical on mobile
   - Include: Years of experience (since 1996), Registered credentials, UCT qualification
   - Simple icon + text format using lucide-react icons (Shield, Award, GraduationCap, Heart)
  </action>
  <verify>
    - All 8 files exist in their directories
    - No TypeScript errors: `npx tsc --noEmit`
    - Each section component imports from correct data file
  </verify>
  <done>
    8 component files created (2 UI primitives, 6 homepage sections). All components are Server Components. Each section imports correct data from src/data/. TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create homepage and services pages</name>
  <files>
    src/app/page.tsx
    src/app/services/page.tsx
    src/app/services/antenatal-classes/page.tsx
    src/app/services/baby-massage/page.tsx
    src/app/services/postnatal-support/page.tsx
  </files>
  <action>
1. Update src/app/page.tsx:
   - Replace placeholder content with all homepage sections
   - Import and render in order: Hero, ServicesOverview, AboutIntro, Testimonials, CTASection, TrustIndicators
   - Each section wrapped appropriately (Hero has no section wrapper, others use Section component)
   - Alternate section backgrounds for visual rhythm (default, muted, default, muted...)

2. Create src/app/services/page.tsx (SERV-01, SERV-05, SERV-06, SERV-07):
   - Server Component with metadata export
   - Title: "Our Services"
   - Import services from @/data/services
   - Grid of all services (similar to homepage but more detailed)
   - Each card shows: name, fullDescription, pricing.amount formatted as "From R{amount}", benefits list (first 3), CTA button
   - Cards for isDetailPage services link to detail pages
   - Cards for non-detail services (vaccinations, lactation, newborn-checkups) show full content inline with anchor IDs
   - Include pricing on every card (SERV-08)
   - Include booking/contact CTA on every card (SERV-09)

3. Create src/app/services/antenatal-classes/page.tsx (SERV-02):
   - Server Component with metadata export
   - Import getServiceBySlug from @/data/services
   - Get service data: const service = getServiceBySlug('antenatal-classes')
   - Display: Hero with service name, full description
   - Benefits section with full benefits list
   - What's included section (service.includes)
   - Pricing section with amount, unit, and what's included
   - CTA section with "Book Your Course" button linking to /schedule
   - Breadcrumb navigation: Home > Services > Antenatal Classes

4. Create src/app/services/baby-massage/page.tsx (SERV-03):
   - Same pattern as antenatal-classes
   - Get service data: getServiceBySlug('baby-massage')
   - CTA: "Book Your Course" to /schedule

5. Create src/app/services/postnatal-support/page.tsx (SERV-04):
   - Same pattern as antenatal-classes
   - Get service data: getServiceBySlug('postnatal-support')
   - CTA: "Get in Touch" to /contact (postnatal support is typically contacted, not directly booked)
  </action>
  <verify>
    - `npm run build` succeeds (pages render at build time)
    - Visit http://localhost:3000 - homepage shows all 6 sections
    - Visit http://localhost:3000/services - services grid displays
    - Visit http://localhost:3000/services/antenatal-classes - detail page with pricing
    - Visit http://localhost:3000/services/baby-massage - detail page with pricing
    - Visit http://localhost:3000/services/postnatal-support - detail page with pricing
    - All pages have proper metadata (check title in browser tab)
  </verify>
  <done>
    Homepage displays all 6 sections (Hero, ServicesOverview, AboutIntro, Testimonials, CTA, TrustIndicators). Services overview page shows all 6 services with pricing. 3 detail pages exist with full content, benefits, pricing, and booking CTAs. All pages have proper metadata. Build succeeds.
  </done>
</task>

</tasks>

<verification>
1. Run `npm run build` - successful build with no errors
2. Run `npm run dev` and verify:
   - Homepage (/) has all 6 sections visible
   - Services link in header navigates to /services
   - Services page shows 6 service cards with pricing
   - Detail page links work (3 detail pages)
   - Each detail page shows pricing and booking CTA
3. Check accessibility:
   - Tab through homepage - all CTAs focusable
   - Heading hierarchy is correct (h1 on each page, h2 for sections)
4. TypeScript: `npx tsc --noEmit` passes
</verification>

<success_criteria>
- Homepage displays all 6 sections with real content from data files
- Services overview page lists all 6 services with pricing
- 3 service detail pages show full information, benefits, and pricing
- Every service has visible pricing (SERV-08)
- Every service has booking/contact CTA (SERV-09)
- Testimonials section shows 3 client reviews (TEST-01 through TEST-05)
- Trust indicators show credentials (HOME-06)
- All pages have proper metadata for SEO
- Navigation works between all pages
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-content-&-pages/02-02-SUMMARY.md`
</output>
