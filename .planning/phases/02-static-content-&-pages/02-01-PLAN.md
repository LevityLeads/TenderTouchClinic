---
phase: 02-static-content-and-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/accordion.tsx
  - src/data/services.ts
  - src/data/testimonials.ts
  - src/data/faq.ts
  - src/data/schedule.ts
  - src/data/about.ts
  - package.json
autonomous: true

must_haves:
  truths:
    - "TypeScript data files export typed arrays/objects for services, testimonials, FAQs, schedule, and about content"
    - "Accordion component renders with keyboard navigation and ARIA attributes"
    - "Data files provide helper functions for data lookup (getServiceBySlug, etc.)"
  artifacts:
    - path: "src/components/ui/accordion.tsx"
      provides: "Radix-based accessible accordion"
      exports: ["Accordion", "AccordionItem", "AccordionTrigger", "AccordionContent"]
    - path: "src/data/services.ts"
      provides: "Service content with pricing"
      exports: ["services", "getServiceBySlug", "Service"]
    - path: "src/data/testimonials.ts"
      provides: "Client testimonials"
      exports: ["testimonials", "Testimonial"]
    - path: "src/data/faq.ts"
      provides: "FAQ questions by category"
      exports: ["faqsByCategory", "FAQItem"]
    - path: "src/data/schedule.ts"
      provides: "Upcoming class dates"
      exports: ["upcomingClasses", "ClassDate"]
    - path: "src/data/about.ts"
      provides: "Megan bio and clinic story"
      exports: ["meganBio", "clinicStory"]
  key_links:
    - from: "src/components/ui/accordion.tsx"
      to: "@radix-ui/react-accordion"
      via: "import"
      pattern: "import.*@radix-ui/react-accordion"
---

<objective>
Create content data infrastructure and accessible accordion component for Phase 2.

Purpose: Establish the foundation for all content pages by centralizing content in typed TypeScript data files and providing a reusable accordion component for the FAQ section. This enables all subsequent plans to import and render content consistently.

Output: 6 TypeScript files (1 component, 5 data files) ready for use by page components.
</objective>

<execution_context>
@/home/codespace/.claude/get-shit-done/workflows/execute-plan.md
@/home/codespace/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-static-content-&-pages/02-RESEARCH.md

# Phase 1 established patterns
@src/components/ui/button.tsx
@src/lib/constants.ts
@src/lib/utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Radix Accordion and create component</name>
  <files>
    package.json
    src/components/ui/accordion.tsx
  </files>
  <action>
1. Install @radix-ui/react-accordion:
   ```bash
   npm install @radix-ui/react-accordion
   ```

2. Create src/components/ui/accordion.tsx following the pattern from RESEARCH.md Pattern 3:
   - "use client" directive (interactive component)
   - Import AccordionPrimitive from @radix-ui/react-accordion
   - Import ChevronDown from lucide-react
   - Import cn from @/lib/utils
   - Export: Accordion, AccordionItem, AccordionTrigger, AccordionContent
   - AccordionItem: border-b border-neutral-200
   - AccordionTrigger: flex, justify-between, py-4, focus-visible ring styles, chevron rotates on open
   - AccordionContent: overflow-hidden, animate open/close
   - Add displayName to all forwardRef components

3. Add accordion animation keyframes to tailwind.config.ts if not present:
   - accordion-down: height 0 to var(--radix-accordion-content-height)
   - accordion-up: height var(--radix-accordion-content-height) to 0
  </action>
  <verify>
    - `npm ls @radix-ui/react-accordion` shows package installed
    - `cat src/components/ui/accordion.tsx` shows all 4 exports
    - No TypeScript errors: `npx tsc --noEmit`
  </verify>
  <done>
    Accordion component exists with Accordion, AccordionItem, AccordionTrigger, AccordionContent exports. Package installed. TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create content data files</name>
  <files>
    src/data/services.ts
    src/data/testimonials.ts
    src/data/faq.ts
    src/data/schedule.ts
    src/data/about.ts
  </files>
  <action>
1. Create src/data/ directory if it doesn't exist.

2. Create src/data/services.ts following RESEARCH.md Pattern 1:
   - Export interface Service with: id, name, slug, shortDescription, fullDescription, benefits[], pricing{amount, currency, unit}, duration?, includes?[], ctaText, ctaHref, isDetailPage
   - Export services array with 6 services:
     - antenatal-classes (isDetailPage: true) - R3500 per couple, 6-week course
     - postnatal-support (isDetailPage: true) - R800 per home visit, R500 clinic
     - baby-massage (isDetailPage: true) - R1200 per 4-week course
     - lactation-consultations (isDetailPage: false) - R600 per session
     - newborn-checkups (isDetailPage: false) - R400 per visit
     - vaccinations (isDetailPage: false) - Price varies
   - Export getServiceBySlug(slug: string) helper function
   - All ctaHref should link to /services/{slug} for detail pages, /services#{id} for sections

3. Create src/data/testimonials.ts:
   - Export interface Testimonial with: id, name, service, quote, imageUrl?
   - Export testimonials array with 5-6 diverse testimonials covering antenatal, baby massage, postnatal support
   - Use placeholder image paths like /images/testimonials/placeholder-1.jpg

4. Create src/data/faq.ts following RESEARCH.md FAQ pattern:
   - Export interface FAQItem with: id, question, answer
   - Export faqsByCategory: Record<string, FAQItem[]> with categories:
     - "Antenatal Classes" (3-4 questions about timing, partners, what to expect)
     - "Baby Massage" (2-3 questions about age, benefits)
     - "Postnatal Support" (2-3 questions about home visits, breastfeeding)
     - "Booking & Payment" (2-3 questions about booking, deposits, cancellation)

5. Create src/data/schedule.ts following RESEARCH.md Pattern 6:
   - Export interface ClassDate with: id, serviceId, serviceName, startDate (ISO string), time, duration, spotsTotal, spotsAvailable, status ('available'|'few-spots'|'full'|'waitlist')
   - Export upcomingClasses array with 4-6 upcoming classes (use dates 1-3 months from now)
   - Include mix of antenatal and baby massage classes with varied availability

6. Create src/data/about.ts:
   - Export meganBio object with: name, title, credentials[], bio (multi-paragraph string), personalNote, imageUrl
   - Export clinicStory object with: founded, location, description, mission
   - Use content from PROJECT.md Context section
   - Use placeholder image path /images/about/megan.jpg
  </action>
  <verify>
    - All 5 data files exist in src/data/
    - No TypeScript errors: `npx tsc --noEmit`
    - Each file exports expected types and data
  </verify>
  <done>
    5 data files created with typed exports: services (6 services + getServiceBySlug), testimonials (5-6 items), faq (4 categories), schedule (4-6 classes), about (meganBio + clinicStory). All files compile without TypeScript errors.
  </done>
</task>

</tasks>

<verification>
1. Run `npm ls @radix-ui/react-accordion` - package installed
2. Run `npx tsc --noEmit` - no TypeScript errors
3. Verify exports:
   - `grep -l "export.*Accordion" src/components/ui/accordion.tsx`
   - `grep -l "export.*services" src/data/services.ts`
   - `grep -l "export.*testimonials" src/data/testimonials.ts`
   - `grep -l "export.*faqsByCategory" src/data/faq.ts`
   - `grep -l "export.*upcomingClasses" src/data/schedule.ts`
   - `grep -l "export.*meganBio" src/data/about.ts`
</verification>

<success_criteria>
- @radix-ui/react-accordion installed
- Accordion component renders with keyboard navigation
- 5 data files with typed exports compile without errors
- getServiceBySlug returns correct service
- All content uses placeholder image paths (actual images not required)
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-content-&-pages/02-01-SUMMARY.md`
</output>
